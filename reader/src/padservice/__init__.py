"""판독 서비스.

``padreader`` 를 HTTP 로 노출하는 얇은 계층이다. 판독 로직은 여기 없다.

앱을 여기서 import 하지 않는다. ``app.py`` 는 모듈을 읽는 시점에 설정을
읽어들이는데, 그것을 패키지 import 에 묶어 두면 ``--help`` 같은 것도 설정
로드를 타고, 설정 오류가 기동 스크립트의 예외 처리보다 먼저 터진다.
uvicorn 은 ``"padservice.app:app"`` 문자열로 받으므로 여기서 노출할
필요도 없다.
"""
