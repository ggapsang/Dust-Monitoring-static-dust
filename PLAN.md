# v3 반영 계획

요구사항 v3(2026-08-15) 대비 현재 판독 모듈의 차이와 고칠 부분.
**아직 코드는 건드리지 않았다.**

---

## 1. 무엇이 달라지나

| 항목 | 현재 | v3 |
|---|---|---|
| 입력 | 사진 2장 (판독 + 기준) | **사진 1장** |
| 기준 | 현장에서 찍은 기준 사진 | **도안 PNG** |
| 칸 측정량 | 정규화 반사율의 **중앙값** | **어두운 픽셀 비율** |
| 조명 정규화 | 앵커 2점 또는 테두리 나눗셈 | 여백 ÷ 테두리 |
| 대표값 | `max`, `pNN` | `max`, `pNN`, **상위 N칸 평균** |

가장 큰 변화는 **측정량**이다. 지금은 칸의 평균적인 밝기를 재는데, v3는
칸 안에서 어두워진 **픽셀이 몇 %인가**를 잰다. 이건 사실상 피복률
(분진 입자가 덮은 면적 비율) 추정이고, 배경 자료의 EAC/AAC 개념과 맞는다.

바꾸면 좋아지는 점이 하나 더 있다. 밝기 평균은 센서 블랙레벨이 조금만
움직여도 같이 움직이는데, 픽셀 비율은 임계를 넘느냐 마느냐라서 잘 안
흔들린다. 분진 입자는 확실히 어둡고 배경은 확실히 밝으므로, 임계가 그
사이에 있는 한 블랙레벨이 다소 변해도 비율은 유지된다. 흰 바탕 패드에서
겪던 편향이 줄어들 가능성이 있다.

---

## 2. 도안 대비 뺄셈이 실제로 하는 일

측정 여백은 인쇄물이 전혀 없는 중간 밴드로 잡혀 있어서, **도안 PNG에서
그 영역의 어두운 픽셀 비율은 0이다.** 따라서 대부분의 칸에서 뺄셈은 값을
바꾸지 않는다.

그래도 v3대로 명시적으로 빼는 것이 맞다. 도안이 바뀌어 여백에 인쇄물이
들어오면 그 칸의 도안값이 0이 아니게 되고, 뺄셈이 그만큼을 자동으로
걷어낸다. 가정을 코드에 숨기지 않고 데이터로 처리하는 쪽이 낫다.

도안 PNG는 잉크가 정확히 0 카운트라 "여백 ÷ 테두리"를 할 수 없다. 도안은
애초에 조명이 걸리지 않은 이미지이므로 정규화 없이 화소값에서 바로 비율을
센다.

---

## 3. 고칠 것

### 3.1 칸 측정량을 어두운 픽셀 비율로 (`cells.py`)

지금은 칸마다 정규화 반사율의 중앙값 하나를 낸다. 이걸 **임계 미만 픽셀의
비율**로 바꾼다.

- 백색 바탕: 정규화값이 임계보다 **낮은** 픽셀의 비율
- 흑색 바탕: 정규화값이 임계보다 **높은** 픽셀의 비율

임계는 설정값으로 빼고 초기값은 비워 둔다. 실증 사진의 분포를 보고 정해야
하는 값이라 지금 짐작해서 넣으면 안 된다. 비워 두면 잠정값을 쓰고 결과에
그 사실을 실어 보낸다.

기존 중앙값도 진단용으로 함께 반환한다. 실증에서 두 지표를 비교해야 한다.

### 3.2 입력을 사진 1장으로 (`pipeline.py`)

`read_pad(사진, 기준사진, tone)` → `read_pad(사진, tone)`.

기준은 규격에서 만든다. `spec` 이름으로 도안을 찾고, 없으면 규격에서 직접
그려 쓴다(생성기가 이미 있다). 도안은 규격마다 한 번만 계산하면 되므로
캐시한다 — 순수 함수라 무상태성은 유지된다.

`baseline_unreadable`, `normalization_mismatch` 사유는 없어진다.

### 3.3 조명 정규화를 테두리 나눗셈으로 고정 (`normalize.py`)

v3는 "여백 밝기를 테두리 밝기로 나눈다"만 말한다. 앵커 2점 경로는 v3에
없다.

**다만 지우지 말고 설정으로 끄자고 제안한다.** 앵커가 있으면 센서 블랙레벨
까지 소거되어 흰 패드 정밀도가 확연히 좋았다(측정치: 블랙레벨 0~25 구간에서
테두리 단일 기준은 스코어가 0.65 이동, 앵커 2점은 불변). 도안 사양이
확정되면 다시 필요할 수 있으므로 코드는 남기고 기본값만 `border_ratio` 로
둔다. 규격도 `v2_protected` 를 남겨 둔다.

### 3.4 대표값에 "상위 N칸 평균" 추가 (`score.py`)

`score.statistic` 에 `topN` 형식(예: `top5`)을 추가한다. `max` 는 이물 하나에
휘둘리고 `pNN` 은 칸 수가 적으면 거칠어서, 그 사이가 필요하다는 요구다.

### 3.5 인쇄물 영역 제외를 사유로 명시 (`cells.py`)

이미 마스크를 계산하고 `masked` 로 배제하고 있다. v3가 명시적으로 요구하니
사유 이름을 `print_element` 로 바꿔 무엇 때문에 빠졌는지 드러나게 한다.

---

## 4. 확인이 필요한 것

**하나.** 앵커 규격(`v2_protected`)을 남길지. 위 3.3의 제안은 "코드는 남기고
기본값만 테두리 나눗셈"이다. 아예 지우라면 지운다.

**둘.** "어두운 픽셀" 임계를 지금 잡을지, 비워 두고 실증 후 정할지. 제안은
비워 두는 쪽이다. 지금 짐작한 값이 들어가면 그게 검증된 값인지 아닌지
구분이 안 된다.

---

## 5. 작업 순서

1. `cells.py` — 어두운 픽셀 비율 산출 + 임계 설정 추가
2. 도안 기준 모듈 — 규격에서 도안을 만들어 칸별 기준값 산출
3. `pipeline.py` — 입력 1장으로 되돌리고 도안 대비 뺄셈 연결
4. `score.py` — `topN` 추가
5. 해당 테스트 정리

각 단계마다 해당 테스트만 돌린다.
